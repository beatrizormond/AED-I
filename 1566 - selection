#include <stdio.h>
#include <stdlib.h>

void Selection (int n, int v[]) {
	int i, j, min, x;
	for (i=0; i < n-1; i++) {
		min = i;
		for (j = i+1; j < n; j++) {
			if (v[j] < v[min]) {
				min = j;
			}

			x = v[i];
			v[i] = v[min];
			v[min] = x;
		}
	}
}


int main() {
	int h, nc, n, i, j;
	int *v, *w;

	scanf("%d", &nc);
	if(nc < 100) {
		for (j = 0; j<nc; j++) {
			scanf("%d", &n);
			if(n > 1 && n < 3000000) {
				v = malloc(n * sizeof(int));
				for (i=0; i<n; i++) {
					scanf("%d", &h);
					if(h>=20 && h<=230) {
						v[i] = h;
					}
				}
				Selection(n, v);

				int primeiro = 1;

				for (i=0; i< n; i++) {
					if(primeiro) {
						printf("%d", v[i]);
						primeiro = 0;
					}
					else {
						printf(" %d", v[i]);
					}
				}
				printf("\n");
			}
		}

	}

	return 0;
}
