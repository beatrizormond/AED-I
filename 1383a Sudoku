#include <stdio.h>

int Analisa_sudoku(int sudoku[9][9]){
    int valor = 0, conta[10], i, j, k, i2, j2;
   
    for (i=0; i<9;i++){  //analisa as linhas da matriz 9x9
        for (k=1; k<=9; k++){ conta[k] = 0;}
        for (j=0; j<9; j++){
            if (sudoku[i][j] >= 1 && sudoku[i][j] <= 9){
                valor = sudoku[i][j];
                if(conta[valor]){
                    return 0;
                }
                else{
                    conta[valor] = 1;
                }
            }
        }
    }

    for (i=0; i<9;i++){ //analisa as colunas da matriz 9x9
        for (k=1; k<=9; k++){ conta[k] = 0;}
        for (j=0; j<9; j++){
            if (sudoku[j][i] >= 1 && sudoku[j][i] <= 9){
                valor = sudoku[j][i];
                if (conta[valor]){
                    return 0;
                }
                else{
                    conta[valor] = 1;
                }
            }
        }
    }

    for (i2 = 0; i2 < 9; i2 += 3){ //analisa cada matriz 3x3 dentro da matriz 9x9
        for (j2 = 0; j2 <9; j2 += 3){
            for (k=1; k<=9; k++){ conta[k] = 0;}
            for (i=0; i<3;i++){
                for (j=0; j<3; j++){
                    if (sudoku[i+i2][j+j2] >= 1 && sudoku[i+i2][j+j2] <= 9){
                        valor = sudoku[i+i2][j+j2];
                        if (conta[valor]){
                            return 0;
                        }
                        else{
                            conta[valor] = 1;
                        }
                    }
                }
            }
        }           
    }

return 1;
}

int main (){
    int sudoku[9][9];
    int n, i, j, x;
    
    scanf("%d", &n);

    for (x = 1; x <= n; x++){
        for (i = 0; i < 9; i++){
            for (j = 0; j < 9; j++){
                scanf("%d", &sudoku[i][j]);
            }
        }
    
        if (Analisa_sudoku(sudoku)){
            printf("Instancia %d\nSIM\n\n", x);   
        }
        else {
            printf("Instancia %d\nNAO\n\n", x);
        }   
    }

    return 0;
}
